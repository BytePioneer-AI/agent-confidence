[project]
name = "agent-confidence"
version = "0.2.0"
description = "Best practices and a reusable skill for business-specific Agent confidence design"
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
